# cron-sync

Mirror backup of `/home/msands/.openclaw/cron` for Git tracking.

- Synced by: `scripts/sync-cron.sh`
- Source of truth: `/home/msands/.openclaw/cron`
- This directory is committed so cron job definitions and run history are backed up in GitHub.
